language: node_js
node_js:
  - "12"
  - "14"
cache:
  yarn: true
  directories:
    - node_modules
script:
  - yarn generate
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: .output/public
  on:
    branch: gh-pages